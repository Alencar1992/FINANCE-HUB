-- Schema aplicado ao projeto FINANCE-HUB.
-- A fonte de verdade da migração completa está registrada no histórico de migrations do Supabase
-- sob o nome initial_finance_hub_schema. Use o Dashboard/CLI para inspecionar ou reproduzir.

-- Tabelas: owners, transactions, obligations, cards, subscriptions,
-- custom_modules e audit_log. Todas possuem RLS e políticas por auth.uid().
